import { HttpClient } from '@angular/common/http';
import { AuthenticationService } from '../../fx-security/services/authentication.service';
import { SecurityConfig } from '../../fx-security/fx-security.module';
import { AuthorizationService } from '../../fx-security/services/authorization.service';
import { BoundarySelectorResource } from '../domain/boundary-selector-resource';
import { Observable } from 'rxjs';
export declare class BoundaryDataService {
    private http;
    private authenticationService;
    private authorizationService;
    securityConfig: SecurityConfig;
    constructor(http: HttpClient, authenticationService: AuthenticationService, authorizationService: AuthorizationService, securityConfig: SecurityConfig);
    getBoundariesForUserScope(): Observable<BoundarySelectorResource[]>;
    filterBoundariesForUserScope(boundarySelectorResources: BoundarySelectorResource[]): BoundarySelectorResource[];
    private getApiUrl;
}
