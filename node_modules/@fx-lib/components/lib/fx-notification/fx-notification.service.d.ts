import { Notification, NotificationsService } from 'angular2-notifications';
import { TranslateService } from '@ngx-translate/core';
import { Router } from '@angular/router';
export declare class FxNotificationService {
    private notifications;
    private translate;
    errorNotifications: Notification[];
    constructor(notifications: NotificationsService, translate: TranslateService, router: Router);
    error(messageType: string): void;
    warn(messageType: string): void;
    info(messageType: string): void;
    success(messageType: string): void;
    httpError(statusCode: number, statusCodeDescriptionsType?: string): void;
    private create;
    private generateNotification;
    private translateMessage;
}
