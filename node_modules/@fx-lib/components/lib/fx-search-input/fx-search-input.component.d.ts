import { EventEmitter, OnDestroy, OnInit, TemplateRef } from '@angular/core';
import { Subject } from 'rxjs';
export declare class FxSearchInputComponent implements OnInit, OnDestroy {
    searchInputValue: string;
    searchFieldMinLength: number;
    required: boolean;
    clearSearchInputWithEscape: boolean;
    placeholder: string;
    requiredAlert: string;
    minLengthAlert: string;
    autocomplete: 'off' | 'on';
    id: string;
    name: string;
    title: string;
    belowSearchInputTemplate: TemplateRef<any>;
    debounceTimeInMilliseconds: number;
    trimSearchInput: boolean;
    style: string;
    searchInputChangedEvent: EventEmitter<string>;
    searchInputChanged: Subject<string>;
    private subscription;
    constructor();
    ngOnInit(): void;
    ngOnDestroy(): void;
}
