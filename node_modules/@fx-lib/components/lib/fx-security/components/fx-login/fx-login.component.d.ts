import { ChangeDetectorRef, OnDestroy, OnInit } from '@angular/core';
import { AuthenticationService } from '../../services/authentication.service';
import { UserService } from '../../services/user.service';
import { IconProp } from '@fortawesome/fontawesome-svg-core';
export declare class FxLoginComponent implements OnInit, OnDestroy {
    private authenticationService;
    private userService;
    private changeDetector;
    btnStyle: string;
    dropDownStyling: string;
    languageSelectorRadioGroupStyling: string;
    icon: string;
    fasUser: IconProp;
    username: string;
    private subscriptions;
    constructor(authenticationService: AuthenticationService, userService: UserService, changeDetector: ChangeDetectorRef);
    ngOnInit(): void;
    isNotLoggedIn(): boolean;
    login(): void;
    ngOnDestroy(): void;
    setIcon(selectedIcon?: string): IconProp;
}
