import { ChangeDetectorRef, OnDestroy, OnInit } from '@angular/core';
import { AuthenticationService } from '../../services/authentication.service';
import { SecurityConfig } from '../../fx-security.module';
import { UserService } from '../../services/user.service';
import { IconProp } from '@fortawesome/fontawesome-svg-core';
export declare class FxUserSelectorComponent implements OnInit, OnDestroy {
    private authenticationService;
    private userService;
    private changeDetector;
    btnStyle: string;
    dropDownStyling: string;
    icon: string;
    fasUser: IconProp;
    username: string;
    private loginSubscription;
    private securityConfig;
    constructor(authenticationService: AuthenticationService, userService: UserService, changeDetector: ChangeDetectorRef, securityConfig: SecurityConfig);
    ngOnInit(): void;
    isNotLoggedIn(): boolean;
    login(): void;
    ngOnDestroy(): void;
    setIcon(selectedIcon?: string): IconProp;
}
