import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { SecurityConfig } from '../../fx-security.module';
export declare class NoPermissionsComponent implements OnInit {
    private router;
    ursLink: string;
    protected securityConfig: SecurityConfig;
    constructor(router: Router, securityConfig: SecurityConfig);
    ngOnInit(): void;
}
