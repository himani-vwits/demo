import { ChangeDetectorRef, OnDestroy, TemplateRef, ViewContainerRef } from '@angular/core';
import { AuthenticationService } from '../services/authentication.service';
export declare class IsLoggedInDirective implements OnDestroy {
    private authService;
    private _viewContainer;
    private changeDetector;
    private _context;
    private _thenTemplateRef;
    private _elseTemplateRef;
    private _thenViewRef;
    private _elseViewRef;
    private loginEventsSubscription;
    constructor(authService: AuthenticationService, _viewContainer: ViewContainerRef, templateRef: TemplateRef<IsLoggedInContext>, changeDetector: ChangeDetectorRef);
    ngOnDestroy(): void;
    hasRole: any;
    hasRoleThen: TemplateRef<IsLoggedInContext>;
    hasRoleElse: TemplateRef<IsLoggedInContext>;
    private _updateView;
}
export declare class IsLoggedInContext {
    $implicit: any;
}
