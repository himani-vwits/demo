import { SecurityConfig } from '../fx-security.module';
import { AuthenticationService } from './authentication.service';
export declare class AuthorizationService {
    private authenticationService;
    securityConfig: SecurityConfig;
    constructor(authenticationService: AuthenticationService, securityConfig: SecurityConfig);
    hasViewRole(): boolean;
    hasViewRoleForBoundary(boundaryValue: string): boolean;
    hasEditRole(): boolean;
    hasEditRoleForBoundary(boundaryValue: string): boolean;
    hasDevopsRole(): boolean;
    hasRole(roleToCheck: string): boolean;
    hasRoleForBoundary(roleToCheck: string, boundaryValue: string): boolean;
    private checkMultipleRoles;
    private checkMultipleRolesForBoundary;
}
