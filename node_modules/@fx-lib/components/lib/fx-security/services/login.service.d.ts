import { SecurityConfig } from '../fx-security.module';
import { HttpClient } from '@angular/common/http';
import { IdpIdentityToken } from './authentication.service';
import { Observable } from 'rxjs';
export declare class LoginService {
    private http;
    securityConfig: SecurityConfig;
    private ursUrl;
    constructor(http: HttpClient, securityConfig: SecurityConfig);
    login(idToken: IdpIdentityToken): Observable<string>;
    private handleError;
}
