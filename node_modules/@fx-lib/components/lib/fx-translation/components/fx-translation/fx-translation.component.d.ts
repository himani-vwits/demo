import { NgbDropdownConfig } from '@ng-bootstrap/ng-bootstrap';
import { TranslateService } from '@ngx-translate/core';
import { Language } from '../fx-language-selector/domain/language';
export declare const FX_LANGUAGE = "FX_LANGUAGE";
export declare class FxTranslationComponent {
    private translateService;
    dropUpStyling: string;
    btnStyle: string;
    btnSize: string;
    readonly languages: Language[];
    constructor(translateService: TranslateService, config: NgbDropdownConfig);
    readonly selectedLanguage: Language;
    select(language: Language): void;
}
