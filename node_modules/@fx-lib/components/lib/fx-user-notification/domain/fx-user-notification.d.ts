export interface FxUserNotification {
    audience?: Map<string, Array<string>>;
    id: string;
    messageHeadline: string;
    messageText: string;
    severity: string;
    updateTimestamp: Date;
    updatedByUser: string;
    read: boolean;
}
export declare const priorityMap: Map<string, number>;
