import { ActivatedRoute, Params } from '@angular/router';
export declare class RouterMock {
    navigate(target: string, params?: Params): Promise<boolean>;
    navigateRelative(target: string, route?: ActivatedRoute): Promise<boolean>;
    navigateBack(route: ActivatedRoute): Promise<boolean>;
}
